# 黄金价格曲线比对预测系统 — 方案 (优化增强版)

## 1. 概述

通过纯价格曲线形态比对与极小自适应双模型集成，将最近约半个交易日（284分钟）的价格曲线与历史上所有相同跨度的曲线进行相似度匹配，找出最具代表性且独立的 3 段历史走势（采用动态 NMS 去重），结合在线学习的极小特征残差模型（Tiny Model），加权预测当前金价未来 6 分钟的曲线与置信区间，并动态生成实盘操作风控建议。

---

## 2. 技术栈

- **后端**：Python Flask
- **数据源**：`yfinance`（XAU/USD 国际现货黄金基准及 CNY 汇率转换）
- **计算库**：NumPy, Pandas, SciPy
- **存储**：SQLite (`gold_prices.db`) 采用 WAL 模式提升并发及 JSON Blob 压缩存储
- **前端**：Chart.js + HTML5/CSS3 响应式看板
- **品种**：国际现货金 (XAU/USD) 换算至人民币/克 (CNY/g)

---

## 3. 数据获取与预处理

| 项目 | 方案与说明 |
|---|---|
| 数据源选择 | **yfinance `GC=F` 及 `CNY=X`**（捕捉国际主流核心趋势并转为人民币计价） |
| 历史范围 | 历史 1 分钟级行情持久化积累至 SQLite，支持通过脚本拉取多年回测 |
| 粒度 | **1 分钟 (1m)** |
| **休市与断层处理** | 过滤跨周末、节假日休市超过 15 分钟的断层片段，每日 06:00 作为交易日分界线 |
| 增量更新与持久化 | 每分钟后台静默定时任务拉取增量追加至 SQLite，不阻塞主 API |

---

## 4. 优化双核心预测机制

### 核心 A: 历史相似度形态匹配 (权重 40%)

**输入**：最近 284 个点（约 4.7h）→ 当前查询曲线 $Q$

**步骤**：
1. **连续交易序列校验**：剔除窗口内包含周末/休市断层的历史切片。
2. **归一化**：$z = \frac{\text{price} - \mu}{\sigma}$。
3. **混合相似度距离度量**：
   - 加权欧氏距离与 Pearson 相关系数距离结合计算，筛选相似度 >= 70% 的有效候选。
4. **动态 NMS (非极大值抑制) Top 3 提取**：
   - 提取独立代表性强、互不重叠（过滤前后 284 半径内重叠）的 Top 3 走势，动态计算 Softmax 温度系数进行自适应权重分配。

### 核心 B: Tiny Model 极小微观特征自适应网络 (权重 60%)

**特征提取**：
- 抽取 RSI(14)、EMA20 乖离率、ATR14 相对比例、多周期短线动量、机构主力行为漂移因子等 8 维微观数据。
**网络结构**：
- 单隐藏层 Numpy 极轻量化前向网络，输出未来 6 步预测残差。
**在线学习 (闭环)**：
- 每次历史预测点时间到期且获取实际开奖价格后，执行实时 SGD 反向传播增量训练（LR 自适应衰减），并在 SQLite 中持久化权重 JSON。

---

## 5. 预测生成与百分比收益率还原

- **预测走势合成**：
  - 以当前实际最新价格 $P_{\text{last}}$ 为起点，将两套核心模型（形态比对 + Tiny Model）生成的相对收益率进行 40:60 比例加权融合（Ensemble Blending）。
  - 根据复利累乘还原预测绝对价格路径，并叠加机构漂移常数平滑。
- **置信区间生成**：
  - 根据 Top 3 历史后续收益率的差异程度与历史总体平均标准差，生成未来 6 分钟价格的**上轨 (Upper Bound)** 与 **下轨 (Lower Bound)** 包络带。
- **盘面建议生成**：
  - 分析日内相对高低点位分位数，综合胜率基准与多空共识占比，动态输出带方向预警的实战操作建议（如 "高位仍有冲高空间，建议逢高止盈"）。

---

## 6. 自纠正引擎与防守防追高闭环

| 模块 | 动作说明 |
|---|---|
| **方向胜率调节系数** | 滑窗提取近 20 次实测胜率。当胜率跌破 50% 时，启动防守系数放大预测区间上限；连胜时调小防守系数。 |
| **MAPE 即时预警监控** | 每分钟检测 6 分钟前的预测路径偏差。若 MAPE $> 0.5\%$，UI 紧急亮红预警“即时重匹配中”，提示强非理性波动。 |
| **极点自适应 EMA 偏差器** | 若模型频繁预测不到实盘的最高低点，积累高/低位 EMA 偏差势能，在下次大波动时做非线性补偿回调修复。 |

---

## 7. 前端展示

- **主趋势线**：最近 284 分钟实际金价（亮绿色细线）
- **预测走势线**：未来 6 分钟主导预测曲线（蓝色虚线/根据胜率渐变发光）
- **置信区间带**：动态防守系数加持构成的半透明包络区域
- **极小自学反馈卡片**：UI 展示在线模型迭代次数、当前学习率与多空共识投票占比，辅以胜率雷达。
- **实时看板**：显示动态建议（看多/看空/平）与胜率。

---

## 8. 项目结构

```
gold-predictor/
├── app.py                 # Flask 主入口 & 后台异步轮询引擎
├── gold_prices.db         # SQLite 数据库（WAL 模式并发支持）
├── src/
│   ├── config.py          # 全局超参数与魔法数字中心
│   ├── db_init.py         # DB Schema 初始化与上下文隔离
│   ├── data_fetcher.py    # yfinance 数据拉取及特征清洗
│   ├── matcher.py         # 传统曲线 Pearson 与欧氏形态匹配核心
│   ├── predictor.py       # 混合集成预测总线与风控模块
│   ├── corrector.py       # 自学习在线反馈与胜率、极值纠偏引擎
│   ├── tiny_model.py      # Numpy JSON 极小轻量前向反馈神经网络
│   ├── utils.py           # 交易日推算及常用数学工具
│   └── charts.py          # ECharts/Chart.js 视图数据组装层
├── static/
│   ├── app.js             # 客户端轮询渲染与自适应缓存控制
│   └── style.css          # Cyberpunk/Neon UI 样式与动画
├── templates/
│   └── index.html         # HUD 实时风控仪表盘
└── scripts/
    ├── clean_and_reload.py
    └── import_2year_history.py
```
